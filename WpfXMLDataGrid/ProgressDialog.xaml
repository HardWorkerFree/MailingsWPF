<Window x:Class="MailingsWPF.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MailingsWPF"
        mc:Ignorable="d"
        Title="Процесс загрузки..." Height="170" Width="250" ResizeMode="NoResize">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="LightBlue" Offset="0"/>
                <GradientStop Color="White" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <ProgressBar x:Name="loadingProgressBar" VerticalAlignment="Top" Height="30" Margin="20"/>
        <Button Content="Отмена" Width="80" Height="40" VerticalAlignment="Bottom" Margin="20" Click="Button_Click"/>
    </Grid>
</Window>
